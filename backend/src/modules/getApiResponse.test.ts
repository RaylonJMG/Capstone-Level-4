import { response } from "express";
import { getApiResponse } from "./getApiResponse";

describe(getApiResponse, () => {
	it("returns a response from the API", () => {
		//ARRANGE

		//ACT
		const result = axios.get(response);

		//ASSERT
	});
	it("returns a response from the API", () => {
		//ARRANGE

		//ACT
		const result = axios.get(response);

		//ASSERT
	});
});
